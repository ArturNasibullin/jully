"use strict";

window.addEventListener("DOMContentLoaded", function () {
  function e(e) {
    var t = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : null;

    var n = function n(e) {
      return Math.floor(e.getBoundingClientRect().top);
    };

    e.preventDefault();
    var o = t ? t.getAttribute("href") : this.getAttribute("href");
    var r = document.querySelector(o);
    if (!r) return;
    var c = n(r);
    window.scrollBy({
      top: c - 30,
      left: 0,
      behavior: "smooth"
    });
    var i = setInterval(function () {
      var e = window.innerHeight + window.pageYOffset >= document.body.offsetHeight - 2;
      (0 === n(r) || e) && (r.tabIndex = "-1", r.focus(), window.history.pushState("", "", o), clearInterval(i));
    }, 100);
  }

  document.querySelectorAll(".navbar-links__link").forEach(function (t) {
    return t.onclick = e;
  });
  var t = document.querySelector(".burger"),
      n = document.querySelector(".navbar-links"),
      o = document.querySelectorAll(".navbar-links__link");
  t.addEventListener("click", function () {
    n.classList.toggle("active"), t.classList.toggle("active"), o.forEach(function (e) {
      e.addEventListener("click", function () {
        t.classList.remove("active"), n.classList.remove("active");
      });
    });
  }), function () {
    var e = document.querySelector(".navbar"),
        t = document.querySelector(".about").offsetTop;
    window.addEventListener("scroll", function () {
      window.pageYOffset > t ? e.classList.add("navbar--fixed") : e.classList.remove("navbar--fixed");
    });
  }();
});
//# sourceMappingURL=main.min.js.map
